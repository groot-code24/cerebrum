# ── Core (required) ─────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
networkx>=3.3
scipy>=1.13.0
scikit-learn>=1.4.0
requests>=2.32.0
tqdm>=4.66.0

# ── Deep learning (optional — enables PyTorch GNN + training) ────────────────
# torch==2.3.0
# torch-geometric==2.5.3
# snntorch==0.9.1

# ── Live visualisation server (optional) ─────────────────────────────────────
# fastapi>=0.110.0
# uvicorn[standard]>=0.28.0
# websockets>=12.0

# ── Experiment tracking (optional) ──────────────────────────────────────────
# mlflow>=2.12.0

# ── Performance (optional — JIT-compiled kernels) ────────────────────────────
# numba>=0.59.0

# ── Development ──────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-cov>=5.0.0
